{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Serve",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/.venv/bin/uvicorn",
            "cwd": "${workspaceFolder}",
            "args": [
                "unitree.app:app"
            ],
            "env": {
                "PYTHONPATH": "${workspaceFolder}"
            }
        }
    ]
}
